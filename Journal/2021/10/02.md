do /etc/systemd/system.conf doplnit zpatky
DefaultTimeoutStartSec=900s

[49, 46, 32, 52, 46, 32, 50, 48, 50, 48]

=== debug bean erroru v qwazar-kb-backend testech ===

- EAPackageService autowiruje EAObjectRepository a EAPackageService
- nejsem ale schopny najit, odkud se EAObjectRepository bere
  - nasel jsem pouze interface, zadnou implementaci
  - prosel jsem vsechny vyskyty a zadny z nich neni bean, implementace interfacu, ani nic podobneho
  - v /actuator/beans vraci:

"EAObjectRepository": {
  "aliases": [],
  "dependencies": [
      "(inner bean)#4f2df47",
      "(inner bean)#2ca19df1",
      "(inner bean)#5d7b91ba",
      "(inner bean)#1ecea476",
      "jpaMappingContext"
  ],
  "resource": null,
  "scope": "singleton",
  "type": "cz.sentica.qwazar.ea.core.repository.EAObjectRepository"
},
"EAPackageRepository": {
    "aliases": [],
    "dependencies": [
        "(inner bean)#5115539a",
        "(inner bean)#1cd9a4f2",
        "(inner bean)#6ebc630a",
        "(inner bean)#9db14b7",
        "jpaMappingContext"
    ],
    "resource": null,
    "scope": "singleton",
    "type": "cz.sentica.qwazar.ea.core.repository.EAPackageRepository"
},

  - coz je zvlastni proto, ze "resource" je null, zatimco u vetsiny ostatnich vypisuje classu,
    kde se definuje @Component, nebo @Bean
    - null ma jeste EAConnectionRepository, EADiagramObjectsRepository, EADiagramRepository,
      EAPropertyRepository, amountRepository, architectureParameterRepository,
      nejspis vsechny repositories
    - databaseLogStore, databaseNominationStore
    - qwazarKB
  - presto ale KB evidentne nabehne

=== veci, ktere se mi nelibi a mozna by se mely pridat do TD ===

- `View.view` field by se mel jmenovat `widgets`
- vsechny widgety dedi z `WidgetBase`, coz znamena, ze v jejich tele jsou opet dostupne vsechny
  funkce DSL, coz velmi casto nedava smysl - napr. `IntegerWidget` by urcite nemel obsahovat tabulku
  - dalo by se resit tak, ze `WidgetBase` sam tyhle funkce obsahovat nebude (a nejspis ani field
    `children`), bude mit jen veci, ktere jsou skutecne spolecne pro vsechny widgety, jako
    `widgetType`, `lid` a mozna `variant`, pak bude nejaky potomek `CompositeWidget`, ktery bude mit
    `children` a builder funkce
  - bezne typy (non-composite) widgetu samozrejme taky muzou mit par builder funkci, ale jen pro
    specificke sub-widgety, ktere mohou obsahovat

- testy ktere padaji na beanech ve worker vlakne
DataOperationControllerIntegrationTest
ExternalDataFlowControllerIntegrationTest
BussinessApplicationCIT
