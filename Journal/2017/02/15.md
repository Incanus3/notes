== dry-types ==

Types::Form::Time.default { Time.now }
- if supplied, default is not used                        - correct
- if not supplied, default is used                        - correct
- if nil supplied, default is used                        - correct
Types::Form::Time.constructor { |t| t && t.round }
- if supplied, constructor is called after type coertion  - correct
- if nil supplied, constructor is called (with nil)       - correct
- if not supplied, constructor is ignored                 - bad - test this on dry-struct level
Types::Form::Time.optional.constructor { |t| t && t.round }
- if supplied, constructor is called BEFORE type coertion - bad
- if nil supplied, constructor is called (with nil)       - correct
- if not supplied, constructor is ignored                 - sad but not that bad - test this on dry-struct level
Types::Form::Time.default { Time.now }.constructor { |t| t.round }
- if supplied, constructor is called BEFORE type coertion - bad
- if nil supplied, default is ignored                     - bad
- if not supplied, default is ignored                     - bad - test this on dry-struct level
