### Sending custom notifications to editable lists of users
#### What we already have
### What we'll need
- `NotificationService.sendCustomNotification()` method
	- can send a notification to recipient, which can be an `EmailReipient`, which is actually a list of to/cc/bcc email addresses - exact