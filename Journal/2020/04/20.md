ssh-keygen
ssh-copy-id jakub@hypervizor
fence_virsh -a hypervizor -l jakub -x -k /root/.ssh/id_rsa -o list
fence_virsh -a hypervizor -l jakub -x -k /root/.ssh/id_rsa -n playground_db3 -o status
fence_pve -a pxc.rfa.cz -l alto-fence@pve -p <pass> -n 136 -o status

pcs stonith create fence_vm_db1 fence_virsh pcmk_host_list="db1" pcmk_host_check="static-list" ipaddr="192.168.60.1" login="jakub" identity_file="$HOME/.ssh/id_rsa" port="playground_db1" power_wait="10"

pcs cluster cib cluster1.xml
pcs -f cluster1.xml resource create pgsqld ocf:heartbeat:pgsqlms    \
    bindir="/usr/lib/postgresql/10/bin"                            \
    pgdata="/etc/postgresql/10/main"                               \
    datadir="/var/lib/postgresql/10/main"                          \
    recovery_template="/etc/postgresql/10/main/recovery.conf.pcmk" \
    pghost="/var/run/postgresql"                                    \
    op start timeout=60s                                            \
    op stop timeout=60s                                             \
    op promote timeout=30s                                          \
    op demote timeout=120s                                          \
    op monitor interval=15s timeout=10s role="Master"               \
    op monitor interval=16s timeout=10s role="Slave"                \
    op notify timeout=60s
pcs -f cluster1.xml resource master pgsql-ha pgsqld notify=true
pcs -f cluster1.xml resource create pgsql-master-ip ocf:heartbeat:IPaddr2 \
    ip=192.168.60.20 cidr_netmask=24 op monitor interval=10s

pcs -f cluster1.xml constraint colocation add pgsql-master-ip with master pgsql-ha INFINITY
pcs -f cluster1.xml constraint order promote pgsql-ha then start pgsql-master-ip symmetrical=false kind=Mandatory
pcs -f cluster1.xml constraint order demote pgsql-ha then stop pgsql-master-ip symmetrical=false kind=Mandatory
pcs cluster cib-push cluster1.xml


# pcs -f cluster1.xml constraint order start pgsql-master-ip then start master pgsql-ha symmetrical=false kind=Mandatory
# pcs -f cluster1.xml constraint order start pgsql-master-ip then promote pgsql-ha symmetrical=false kind=Mandatory

# pcs resource meta pgsql-ha master-max=1




# - nezapomenout na tmpfile:
cat <<EOF > /etc/tmpfiles.d/postgresql-part.conf
# Directory for PostgreSQL temp stat files
d /var/run/postgresql/10-main.pg_stat_tmp 0700 postgres postgres - -
EOF

# - pocatecni replikaci udelat pod spravnym application_name a z ip, ktera bude potom clusterova



pcs constraint order promote postgres-ha then start postgres-master-ip id=promote-postgres-ha-than-start-postgres-master-ip kind=mandatory symmetrical=false


- pri problemech s promotem zkusit
https://github.com/ClusterLabs/PAF/issues/80#issuecomment-284985300
- a
crm_simulate -sL
- a
https://github.com/ClusterLabs/PAF/issues/80#issuecomment-286058241



https://www.unixarena.com/series/pacemaker-corosync/
https://www.alteeve.com/w/Main_Page

- na masteru uzavirat wal segmenty po 5 min a uchovavat jich tolik aby pokryly den, v noci delat
  basebackup a pripadne ho rozkopirovat na slavy, pokud to ma smysl

- vyzkouset v pribehu zapisu prerusit sit mezi db, ale ne z app na db

podivat se na:
- monitoring
- troubleshooting
- barman
- pgbackrest
- pg_archivecleanup - pouzit pro cisteni archive_dir po kazdonocnim basebackupu
- pgbench



- zbavit se geerlingguy.ruby - instaluje ruby globalne, tudiz pak prebije systemove a nelze zmenit
