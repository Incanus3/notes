=== Wed, 4.5. ===

- 15.00-20.00 - globus-bonus
  - pridano filtrovani a strankovani artiklu

  - tornado-utils
    DeclarativeBase.where now filters using LIKE if column and value are strings and value contains '%'
    bound metadata to engine
    added colors to logging
    bumped version

    ResourcesHandler.get() now orders records by id
    - added DeclarativeBase.where_query - returns query after condition transformation, before calling
      all()
    bumped patch version

  - frontend
    added sass support
    - added .ruby-version, .ruby-gemset and Gemfile with sass
    - added watch-sass.sh script, renamed develop.sh to watch-browserify.sh
    - assets/css/styles is now scss, css is generated

    articles can now be filtered by name
    - added inline form to ArticleManager, added filter state and event handlers
    - added GroupManagement.filterArticles, passed to ArticleManager as onFilter
    - added Model.where and ModelProxy.where
    fixed generated remove<related model> generated by hasMany

    articles can now be filtered by both name and gastro group
    filters can be cancelled
    fixed Model.where
    - snake-cased attribute names
    - fixed utils.mapObject
    fixed ModelProxy.where
    - fixed utils.mergeMaps

    cleaned up UI
    - added colors to buttons
    - switched confirm and cancel buttons in create/update forms

    articles are now paginated by 10 per page
    fixed utils.numSort - it and thus arrayEqual were destructive
