=== Wed, 18.5. ===

- 10.30-11.30, 13.30-20
  simplified entity action creators
  - extracted createAsyncAction generic action creator
  - used by most entity actions and also by createLoaderAction

  minor refactorings
  - article filtering moved from ArticleManager to group_management_store, it is now a function of
    state
  - selectedGroup is no longer called from inside of components, instead it is a function of state and
    is called from mapStateToProps
  - deleted old notes

  pagination is now persisted in store
  - first visible article is now correctly selected after filtering or changing page

  WIP: action management now renders again
  - most functionality is disabled
  - added loader actions and reducers for all entities
  - loadGroupManagementData action replaced by loadAllData
  - added selectAction and resetActionSelection actions and reducers
  - connected ActionManagement to store, removed state
  - fixed createLoaderAction to properly convert multiword model names
  - fixed Action.withBuildings to return promise instead of taking callback
  - removed ActionRepository and Repository - no longer needed

  ActionForm renders again
  - entities are passed from state through ActionManagement and ActionList to ActionForm

  action creation works again

  action updating and deletion works again
  - selected action is now passed to ActionManagement in props

  changing action buildings works again
  updating action no longer loses its buildings

  moved logic from ActionManagement to ActionList, ActionBuildings and ActionDetail
  - ActionManagement component is now stateless

  ActionManagement component is now trivial - moved to main.coffee
  simplified ActionList.createAction using _.assign

  handled unique constraint violation in createAction and updateAction
  removed unneeded requires of ReactDOM

  build-production.sh now also compiles scss

  fixed pagination when many articles are present

  article filter form now retains filter values

[ ] artikly nejsou razeny podle id, prestoze backend je vraci ve spravnem poradi
[ ] pridat skript na naplneni datastore entitami
[ ] vygenerovat spoustu artiklu a otestovat rychlost aplikace
[ ] opravit aktualizaci url
