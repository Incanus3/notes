=== Fri, 15.4. ===

- 14.30-19.30 - pokracovani ve vyvoji globus bonus
    cleanup and file splitting
    - used explicit stateless components where possible
    - used CS list comprehention to generate articles
    - split and renamed article_group_manager.coffee
      -> group_table.coffee, group_form.coffee, group_manager.coffee
    columns are now one above the other on xs displays
    added TODO notes

    article ids no longer need to correspond to position in @state.articles
    - stored in Map
    - added map utils
    fixed article in group ordering

    group ids no longer need to correspond to position in @state.groups
    - groups are stored in a Map indexed by group id
    groups can be deleted
    - handled cases when no more groups remain

    group update form is not shown when no group exists
    fixed created group id when no group existed
    newly created group is immediately selected
    article in group selection is reset after group removal
    added colors to group management buttons

= 5h
