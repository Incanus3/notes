=== Fri, 22.4. ===

- 11-12
  - globus-bonus-backend
    added OPTIONS method support to ArticlesInGroupHandler
    POST and DELETE to /groups/:group_id/articles/:article_id now return 'OK' instead of empty body so
      that it's a valid json
    groups returned by GET /groups/with_articles are now sorted by id

  - globus-bonus-frontend
    group management is now completely persistent
    - article group membership is loaded along with groups
    - addition/removal of article to/from group is persisted

- 3-4
  - globus-bonus-frontend
    prepared for two-page view with navigation
    - added GlobusBonus component, rendered as main component
    - renders navbar with two links which change state.page
    - renders either group or action management depending on state.page

    moved group management components into subdirectory to separate them from action management
      components
    moved ActionManagement to separate file in components/action_management/

  - stared designing action management page
