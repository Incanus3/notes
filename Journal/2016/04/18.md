=== Mon, 18.4. ===

- 10.30-12.30 - globus-bonus
  buttons are disabled when it makes sense
  - when no groups exist, edit group, delete group and add/remove article to/from group disabled
  - when no articles exist, add to group disabled
  - when no articles in group, remove from group disabled

  selected article/group/article in group ids are properly initialized
  - when there are now articles/groups/articles in the first group, initial id is set to null instead
    of 0 as before
  added utils.and and utils.or as laternatives to built-in and/or which consider 0 as truthy value

- 15.45-20.45
  - globus-bonus-backend:
    initial commit

    added Group model, migration and handlers

  - globus-bonus-frontend:
    GroupForm submit is disabled when name field is empty

    articles are now loaded from backend
    - groups are no longer predefined

    groups are now loaded from backend

    extracted common Article and Group behavior (load) into common Model superclass

    moved most of the sources to src/models and src/components

    new groups are now persisted
    - added Model class methods:
      - create, update, persist, exists
      - to be overridden - serialize
      - helpers - ajax
    - renamed Model.load to .all
