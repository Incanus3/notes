=== Sat, 11.6. ===

- 5.30-7.30
  - oprava iniskriptu na testingu
    - status_of_proc v nekterych pripadech nefungoval
      - volat stylem status_of_proc -p "$pidfile" "" "$name"
      - volani pidofproc ze status_of_proc - quotovat parametry
        - quotovat by se mozna nemelo, protoze pak se -p pidfile bere jako jeden parametr
      - v pidofproc
        - osekat whitespace z $pidfile - treba pouzitim echo $pidfile | xargs
        - test na pocet pozicnich parametru (if [$# -ne 1]) vnorit do if [ ! "$specified" ], jinak
          rve invalid arguments pokud nedostal base (pro odvozeni pidfile), i kdyz dostal primo
          cestu k pidfile
        - testy existence procesu - druha vetev - test volanim ps zde nefunguje, nebot ve 14.04
          ps -p $pid vraci status 0 i kdyz proces neexistuje
        -> nahradit test
           ps "${pid:-}" >/dev/null 2>&1
           za
           [ -n "$(ps -p ${pid:-} -o pid=)" ]

- 7.30-10.30
  - v globus-bonus se rozbilo zachovani stavu pri prechodu mezi routami
    - problem je, ze pri prechodu se zavola onLocationChange, ktery inicializuje stav z parametru
      (coz potrebujeme, aby fungovalo back/forward)
    - jak ale odlisit back/forward od prechodu mezi routami?
    - a jak je vubec mozne, ze driv fungovalo?
    - Router.onUpdate nelze pouzit - vola se s kazdou zmenou url
    - Route.onEnter, Route.onLeave nezle pouzit - volaji se pozde
    -> nelze poznat podle location, nebot tam neni rozdil
  - opraveno - navbar link vola ignoreNextLocationChange()

= 5h
