=== Wed, 18.3. ===

- prace - alto
  - 10.30-11.30
  - 3-7
  - 7.30-10.30

  = 8h

== updated alto-model-utils ==
shortcut_attribute doesn't add validation if attribute name is the same as wrapped attribute name
shortcut_attribute - predicates and scopes are now opt-out
renamed alto-ruby-utils to alto-model-utils
added QualifiedNotFoundError
- required in alto-model-utils.rb
- added spec

== created alto-ruby-domain ==
initial commit
added client model
added activevalidators as dependency
added basic spec for client
- not testing functionality provided by alto-module-utils
- added testing gems, helpers, database config

== created alto-controller-utils ==
initial commit

added crud controller

== updated objednavky ==
Client#status is now shortcut_attribute
- added translations, decorator methods, form field
moved relations to app specific section

updated shortcut_attribute calls after updating alto-ruby-utils
- predicates and scopes are now opt-out

added translations of model names and crud controller messages
extracted devise, simple_form and wice_grid localization from cs.yml

extracted Admin::CrudController
- subclassed by all CRUD controllers, they must provide only:
  - Model, PermittedParams, [IncludeInGrid], [DependentRecordsErrorMessage] constants
  - optional before_save hook
  - non-standard actions
  - additional exposes
- updated views

changed CrudController from class to module
- otherwise we can't inherit from other customized controller, like ApplicationController

moved CrudController to alto-controller-utils repo
added it as dependency

extracted QualifiedNotFoundError
- used in all models
- fixed spec
fixed Transaction after changing AttributeAliasing.wrap_attribute to
  Attributeshortcutting.shortcut_attribute

moved QualifiedNotFoundError to alto-model-utils
- updated dependency

extracted common Client logic to alto-ruby-domain
- Client now inherits from Alto::Client
- moved general specs there
