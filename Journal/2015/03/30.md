=== 30.3. ===

- prace - alto
  - 2-2.30
    - view-utils

    added NavbarHelper#navbar_tab and #admin_navbar_tab helpers

    - objednavky

    navbar tabs can now be hidden in configuration
    - updated view-utils after adding navbar_tab and admin_navbar_tab helpers
    - overrided navbar_tab to show only if configured
    - used in navbar layout partial
    - updated labels for id, client_id, card_id

  - 3.15-6.15
    - ruby-domain

    added uniqueness validation to client_id and card_id

    - rails-auth

    added validation errors to user registration when user wasn't found or was already registered

    - objednavky

    updated rails-auth after fixing validation errors in registration form
    fixed test suite after
    - updating registration to find client by client_id with prefix
    - adding validations to client_id and card_id and chaing their labels
    - changing registration context to depend upon config, not subdomain
    - rails started sorting by descendant id by default
    - rails stopped noticing has_many association changes before reload

  - 8.45-3.45

  = 10h30
