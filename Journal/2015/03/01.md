=== Sun, 1.3. ===

- prace - alto
  - 3-8
    BillsHandler now ensures presence of 'header', 'items' and 'payments' keys
    added required fields to BillHeaders

    BaseHandler now returns 400 'failed to parse body as json' for malformed json or non-utf encoding
    BaseHandler now ensures that attributes passed to create/update are dict

    - finalni rozbehnuti redmine

    BaseHandler now returns 400 'failed to parse body as json' for malformed json or non-utf encoding
    BaseHandler now ensures that attributes passed to create/update are dict

    added Transactions model, handlers and routes

    added Meals model, handlers and routes
    filtering by month now works for Date columns too

    updated info page

    added Orders model, handlers and routes
    updated info page

  - 9-10
    implemented /canteens/:id/meals route (without filtering for now)
    moved chain and canteen handlers around

    implemented /clients/:id/transactions route (for now without filtering)
    moved client routes around

    implemented /clients/:id/orders and /meals/:id/orders routes (for now without filtering)
    moved meal routes around

    updated info page
