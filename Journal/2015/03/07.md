=== Sat, 7.3. ===

- prace - alto
  - 3.30-4
  - 5-6.30
    updated generic /clients routes to use ORM Client model
    - added scoped Session to models.connection
    - added DeclarativeBase - ORM models will subclass this
      - date filtering not updated yet
    - added Client and Employer ORM models
    - updated crud handlers - create, update and delete to use ORM models
    - updated BaseHandler.render to handle ORM objects
    - updated BaseHandler.finish to close Session
    - updated BaseFactory to use ORM models
    - updated ClientsTest

  - 7-10.30
    updated generic /canteens routes to use ORM models
    - added Canteen and Chain ORM models
    - updated handlers
    - updated factories
    - updated CanteensTest

    updated generic /chains routes to use ORM models
    - updated handlers
    - updated ChainsTest

    updated generic /transactions routes to use ORM models
    - updated handlers
    - added Transaction ORM model
    - added TransactionFactory
    - added TransactionsTest
    updated EmployersTest

    updated generic /orders routes to use ORM models
    - updated handlers
    - added Meal and Order ORM models
    - added MealFactory and OrderFactory
    - added OrdersTest
    moved database cleaning to TestCase

    updated generic /meals routes to use ORM models
    - updated handlers
    - added MealsTest

    fixed error handling
    - handler checks whether record was found before updating
    - model checks required fields in create

    updated /clients/:id/transactions and /clients/:id/orders to use ORM models
    - added relationships to models
    - updated handlers
    - added ClientTransactionsTest and ClientOrdersTest
    Base.all now orders by id

    updated /canteens/:id/chains and /chains/:id/canteens to use ORM models
    - updated handlers
    - added ChainCanteenRelation ORM model
    - added relationships to Canteen and Chain
    - added CanteenChainRelationsTest

    updated /canteens/:id/meals to use ORM models
    - updated handler
    - added relationships to models
    - added CanteenMealsTest

    updated /meals/:id/orders to use ORM models
    - updated handler
    - added relationships
    - added MealOrdersTest

  - 11-2
    /canteens/:id/clients results now include relation_type
    - added ClientCanteenRelation model
    - added Canteen.client_relations()
    - updated handler
    - added CanteenClientsTest
    updated DeclarativeBase
    - .create now passes positional arguments to constructor
    - moved ensure_required_fields call to constructor

    added /clients/:id/canteens, including relation_type
    - added Client.canteen_relations()
    - added ClientCanteensHandler
    - added CanteenClientRelationsTest.test_client_canteens

    deleted old models

    added /clients/:id/chains and /chains/:id/clients routes
    - added ClientChainRelation ORM model
    - added relationships
    - added Chain.client_relations()
    - added Client.chain_relations()
    - added ChainClientsHandler and ClientChainsHandler
    - added ChainClientRelationsTest
    fixed circular dependencies
    added ordering to joins

    - pokus o prepis Bills na ORM
