=== Thu, 26.3. ===

- prace - alto
  - 2.30-4 - komunikace s evzou
  - 4.15-4.45, 5.15-5.45 - sepsani pozadavku
  - 6.30-10.30
    - ruby-domain

    added client_id presence validation
    updated locales

    - rails-auth

    changed card_id to client_id in all occurrences
    - card_id is written on card chip
    - client_id is written on the card and used as identification

    removed subdomain from RegistrationContext
    - context is now completely generic
    - updated call from RegistrationsController

    client signature (shown in user bar) is name instead of client_id

    updated registration
    - rejects if client with given id is already assigned to confirmed user

    added flash messages and client_id field errors for various cases of registration failure

    - objednavky

    updated rails-auth and ruby-domain dependencies
    - handling registration failures - client_id not found or already registered
    - validating client_id presence
    RegistrationContext now inferrs chain from Config.chain_prefix, instead of subdomain
    RegistrationsController finds client by given card number and prefix
    - added Client.find_by_card(prefix, card_number)
    added config.yml to linked files
    updated locales and seeds

  = 6h30
