=== Wed, 11.2. ===

- prace - alto
  - 11-12
    - hledani potvrzeni o studiu, domluva s evzou
    - testovani transakci v datastore
    - pokracovani ve zjednoduseni vztahu retezec-jidelna-klient

    improved /clients/:id/transactions/last
    - returns 404 for nonexistent client, [] when no transactions
    - added BaseHandler.join_first()
    - used in ClientLastTransactionHandler.get()

    transaction client_id is now mandatory

  - 2-3, 3.30-7.30, 7.45-9
    - komunikace s evzou ohledne zmeny struktury transakci
    - pridani a oprava migraci na novou strukturu
    - rozbehnuti migraci v datastore po pridani schema_plus
    - migrace db na serveru

    - datastore:

    transaction client_id is now mandatory

    updated Transaction model after changing table schema
    - renamed columns
    - added null constraints
    - added server_default to transacted_at
    extracted handlers from handlers.resources to separate files
    updated TransactionsHandler.get() to compute new balance from last transaction and amount

    added server_default to canteen.comment
    added server_default to canteen deadlines
    fixed server_defaults for canteen deadlines

    WIP: chain-canteen relationship is now 1:N
    - updated models, chain_id is now required field for canteen

    WIP: removed client-canteen relations

    WIP: removed chain-canteen and client-canteen routes and join models
    - direct client-canteen relations are no longer possible
    - chain-canteen relationship is now 1:N, so no joins are needed
    added foreign key definition from canteen to chain

    restored /chains/:id/canteens route, but it no longer uses join table
    removed circular dependencies using lambdas
    shortened import module names
    fixed tests after changes to chain-canteen-client relationships and transactions

    updated info page

  - 12.45-1
    - objednavky:
    WIP: simplifying chain-canteen-client
    - updated models - removed join models, updated relations

    WIP: simplifying chain-canteen-client
    - removed add/remove_canteen from ClientRelationsController and its tests

  = 7h15
