=== Sun, 21.6. ===

- 12-3 - rezervace (auth)
- 3.30-6.30 - rezervace (auth)
  - reseni povinnych registracnich poli
  - oprava validace v alto-rails-auth
- 7.30-10 - rezervace (auth)

- rezervace

  updated required registration fields …
  - updated alto-rails-auth - registration context fields are now structured
  - updated registration contexts
  - added validations to Client
  - updated client views after changes to company field names
  added basic user administration - only index and destroy
  updated makro stylesheet
  - wice grid column header links had the same background and foreground color
  - used image-url helper

  split client_id input for makro client registration

- alto-rails-auth

  RegistrationController: fixed passing of context to resource.validate

  updated RegistrationContext …
  - render_field?, required_field? and disabled_field? now takes fieldset into consideration
  - fields, required_fields and disabled_fields are now hashes with list values, not plain lists
  added name field to adress fieldsets
  removed no longer needed comp_ prefixes

  updated RegistrationsController#handle_failure …
  - moved clean_up_passswords call after validate call, otherwise 'password too short' error is
    overwirtten by 'password can not be empty'

  added translation for address.name

  enabled customization of client_id field …
  - extracted RegistrationsController#client_error_field
  - extracted RegistrationContextDecorator#client_id_field
  - used in client_fields partial

- alto-ruby-domain

  removed no longer needed address_ and comp_ prefixes

  removed client_id and card_id uniqueness validation …
  - must be unique only with respect to one chain, which we can't validate, since it's a many-to-many
    relationship

- objednavky

  updated RegistrationContext …
  - upgraded dependencies
  - shown/required fields are now structured
  - updated client views and specs
  upgraded to rails 4.2.2

= 8h30
