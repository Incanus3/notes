=== Thu, 30.7. ===

- 9.45-10.45 - makro - refactor plateb
  Bills#generate_immediately and #update_immediately now deletes bill/bill_update upon failure
  - it also uses distinct 'manual' state for these records - they won't be picked up by background
    sender

  - casovy odhad implementace 'modulu' pro AZ

- 11.45-4.45 - makro - platby - error handling
  - testovani handlingu timeoutu po neuspesne platbe
    improved error handling for payments#handle
    - updated csob-payment-gw - session expired is considered a successful response
    -> Payments.process returns PaymentFailure for this instead of RequestFailure
    - handled PaymentFailure with status = rejected in payments#handle
    - added #payment_fail, #payment_abort and #payment_reverse to OrdersMailer + views
    - calling these from Orders service
    Payments.process returns Succes for accepted payment (in addition to queued)
    added payment_id to RequestFailure

  - nasazeni na testing
    fixed db seeds
    - fixed problem with isolation level for nested transactions

    added logging to Bills#update

= 6h
