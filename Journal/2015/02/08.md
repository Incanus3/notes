=== Sun, 8.2. ===

- prace - email-machine
  - 11.45-2.45 - finalni refactoring xml importu

== Full vs. diff import ==
- ContactList#valid_from
  - ContactList
    - Customer.clear_neo_importing_contact_list(contact_list_id) if contact_list.valid_from
    - Customer.mark_customers_deleted(cl_id)                     if contact_list.valid_from
    - ContactList#transfer_contacts                              if contact_list.valid_from
  - Customer
    - connects neo_importing_cl with customer if     contact_list.valid_from
    - connects neo_cl           with customer unless contact_list.valid_from

- ContactList#do_full_import? (clear_before_import? || !valid_from)
  - Customer.mark_deleted(customer_id) unless contact_list.do_full_import?
  - finds and uses existing Customer   unless contact_list.do_full_import?
