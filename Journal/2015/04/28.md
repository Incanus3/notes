=== Tue, 28.4. ===

- prace - alto
  - 1.30-3 - uprava updated_at klientu pro jq, opetovna kontrola gpe klienta a komunikace s dubem
  - 3-5 - hrani s atomickym fetch and add sequenci
  - 5-6.30 - pridani alembicu, override alchemy_url, migrace pro sequence
    added alembic
    - added dependency to requirements
    - initialized alembic structure
    - modified migrations/env.py to set sqlachemy.url from models.connection.url
    globals.env is now set from DS_ENV environment variable if present
    - this way we can migrate db for specific environment
    implemented (not yet safe) Sequence class with inc_and_return method
    - added 'create sequences table' migration
    - added basic threading test

  - 6.30-7.30
    Sequence.inc_and_return now retries upon insert colision
    - models.connection.engine is now fully qualified so that it may be overrided in test
    - test creates new engine with large thread pool so that insert conflict is more likely
    changed engine database configuration key to driver

    upgraded capistrano deploy to install dependencies and run migrations

  - 8-11
    BillHeader constructor now accepts sequence name instead of bill number
    - when bill number is not given, but sequence name is, it's used to assign the bill number
    - tested

    changed sk_fin.c_uctu to bigint

  = 9h
