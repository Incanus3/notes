=== Tue, 11.8. ===

- 10-11 - makro - deploy
  - nasazeni aktualizace rezervaci na makro server, opetovne zobrazeni stavu objednavky v historii
    showing order state column in order history again
    - updated state translations
    updated deploy config

- 16.30-19.30 - objednavky
  - config by mel byt rozdelen podle environmentu - testy vyzaduji konkretni retezec, nebot pocitaji
    s konkretnimi fieldy v registraci
  - oprava registrace a testu
    fixed registration errors
    - Client.find_by_card now raises Client::NotFound, added bang, updated RegistrationsController
    - updated rails-auth - errors on client_id are replaced, not added
    - User password is not required in administration context
    fixed a ton of specs
    reverted temporary confirmation fix - unconfirmed access was allowed for 1 month
    updated dependencies
    reinstalled wice_grid - config, stylesheets
    removed datastore tables from db schema

- 4h - studium ROM

= 8h

=== ROM ===
== Registry ==
- includes Enumerable
- delegates each to elements
- interprets methods with name present as key in elements as elements[name] access
- what subclasses this?
  - RelationRegistry

== Options ==
- simplifies attibute population from hash during construction:
class User
  include Options

  option :name, type: String, reader: true
  option :admin, allow: [true, false], reader: true, default: false

  def initialize(options={})
    super
  end
end

user = User.new(name: 'Piotr')
user.name # => "Piotr"
user.admin # => false

== ClassMacros ==
- defines 'defines' macro used for class-level settings
class MyClass
  extend ROM::ClassMacros

  defines :one, :two

  one 1
  two 2
end

class OtherClass < MyClass
  two 'two'
end

MyClass.one # => 1
MyClass.two # => 2

OtherClass.one # => 1
OtherClass.two # => 'two'

== Plugin ==
- holds module and options, Plugin#apply_to(klass) includes module in class

== Plugins::Relation::RegistryReader ==
- adds class option __registry__
- defines method_missing to interpret unknown methods as registry fetches

