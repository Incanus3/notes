=== Wed, 28.5. ===

- prace - fs
  - 12-2
    - odfiltrovani poslednich nezajimavych eventu z logstash
    - studium a oprava pravidel pro omezeni ssh spojeni

  - 3.45-4
    - reseni problemu s awstats na akfrydlant (opakovany log v mailu)

  - 4-4.15
    - reseni problemu s nfs na testing (df zamrzne, protoze nefunguje mount
      /var/www) - po spusteni fw (ktery nebyl puvodne povolen) chybelo pravidlo
      pro nas

    - uvolneni mista na testing a lb1

  - 4.15-4.30
    - omezeni poctu ssh spojeni na nix, dev, eye, production, testing
      - akfrydlant delat opetrne

  - 4.30-6.45
    instead of starting manager in separate thread and sending messages to it,
    tests now call MessageHandler#handle_message directly
    - simpler and much faster
    - not that close to real environment, but that's what integration-testing
      repo is for
    only two tests need to start manager - these test correct logging of message
      server connection failure
    removed 'running manager' and 'side channel' helpers
    all shared stuff used only in one spec file moved there (increase locallity)
    MessageHandler - Consumer initialization moved from start to constructor

  = 5h

- akfrydlant - je treba nastavit omezeni na pocet ssh spojeni, firewall zatim
  ale neni vubec povolen - je treba spustit a nechat honzu zkontrolovat, zda jde
  vse, co ma
