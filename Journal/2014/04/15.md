=== Tue, 15.4. ===

- prace - fs
  - 5-6
    - added Tasks::Web::Group to handle manipulation of groups for hostings

    - added MngObjects::Account which calls Tasks::Web::Group

  - 6.45-7
    - zaktualizovan management na webserverech, vytvoren skript pro vytvoreni
      accountu na testingu, otestovan

  - 7.45-9
    - System::Groups module turned into System::Group class
      - functionality is growing and state needs to be passed btw methods
      - original class methods are still supported
    - System::Group.create now raises NameExists, GidExists and PermissionDenied
    - specs and callers updated

    - Web::Group.create catches System::Group::GidExists and NameExists
      and raises Web::Group::SystemGroupExists
    - specs updated

    - MngObjects::Account#create now catches
      Tasks::Web::Group::SystemGroupExists and raises Warning
    - specs updated

  - 10.30-11.45
    - added Web::Hosting.delete and Web::VirtualHost.delete
      - updated specs

    - implemented WebHosting.delete by calling Tasks::Web::Hosting.delete

  - 12.45-1.15
    - added Web::Group.delete
  = 4h15

- smazani webhostingu smaze uzivatele a smazani accountu smaze skupinu, data ale
  zustavaji. je to v poradku?
  - data by zustat mela
  - je lepsi ponechat usery, nebo data bez vlastnika?
- je vubec akce smazani hostingu/klienta k necemu uzitecna?
  - hodi se minimalne na testovani, tam by ale bylo idealni smazat data




- rewatch destroy all sw video on isolation-integration testing - test to avoid
  fragility of isolated test

== CoffeeScript ==
http://pragdave.me/blog/2014/01/28/class-factories-in-coffeescript/

class Widget
    # class method
    @for_element: (el) ->
        type = el.data("type")
        # dynamically fetch nested class and call its constructor
        new @[type](el)

    constructor: (@el) ->

class Widget.Wibble extends Widget
    constructor: (el) ->
        # subclass-specific stuff
        superclass Widget.Wobble extends Widget
    constructor: (el) ->
        # subclass-specific stuff
        super

