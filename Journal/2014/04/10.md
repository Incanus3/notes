=== Thu, 10.4. ===

- prace - fs
  - 12.45-1.45 - extracted VirtualHost class from Webhosting
  - 1.45-2.30 - extracted VirtualHost spec from Webhosting spec
  - 3.15-4.15
    - moved VirtualHost class, spec and template to tasks
      - updated MngObjects::WebHosting to reference it
      - updated WebHosting spec
      - stubbed calls to tasks - create_user and VirtualHost.create
      - added message expectations for these
      - group creation and deletion and user deletion is no longer necessary
    - added Tasks::WebHosting::VirtualHost extracted from server-management
      - added spec for it
      - added virtual host template
  - 5.15-5.30
    - Tasks::System::Users#create_user and #create_group no longer takes uid and
      gid, instead it computes it from username and groupname
    - updated Webhosting to call create_user and create_dir with username and
      groupname
  - 6.30-8
    - System::Files#create_dir now takes username and groupname instead of uid
      and gid

    - extracted System::Groups from System::Users
      - adjusted method names
    - Users and Groups methods are now module methods - the modules can be no
      longer included
    - updated spec

    - added WebHosting class extracted from server-management
    - VirtualHost is now nested inside it
    - VirtualHost and WebHosting inherit from Struct
      - initialize is no longer needed
      - updated template to use readers instead of instance variables
    - added WebHosting spec
    - updated VirtualHost spec
    - moved Groups from users.rb

  - 8-8.45
    - System::Files#create_dir is now module_function
      - updated Webhosting#create_subdirs to call it properly
      - removed unneeded includes of modules from WebHosting
      - updated Files spec
    - removed unused Server::System
    - stubbed User and VirtualHost creation in WebHosting spec
    - updated MngObjects::WebHosting to use Tasks::WebHosting
      - stubbed Tasks::WebHosting in MngObjects::WebHosting spec

  - 9-10
    - nasazeni tasks na mysql1-slave2-1
      - aktualizace rvm
      - instalace ruby
      - vytvoreni skriptu
      - vytvoreni mysql usera
      - oprava create_db - pridani quotu

  - 11-11.45
    - nasazeni tasks na web1b
      - instalace rvm a ruby
      - vytvoreni skriptu create-webhosting
      - oprava drobnych chyb
  = 7h

== JavaScript testing ==
https://github.com/modeset/teaspoon
- Javascript test runner built for Rails. It runs tests in the browser or
  headless using PhantomJS or Selenium WebDriver.

https://github.com/dockyard/ember-appkit-rails

- jasmine tactics - 47 min
