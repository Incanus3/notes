=== Mon, 21. 4. ===

- prace - alto
  - 2-2.15
    optimized ClientCanteenRelation.exists?
    - it no longer computes all client's relations just to see, if the given on
      among them
    - added Client#has_canteen_relation? and #has_chain_relation?
    - added Chain#has_canteen?
    - tested ClientCanteenRelation.exists?
  - 2.15-3.15
    - refaktor a testovani ClientCanteenRelation, zjistovani dopadu, oprava
      nekterych

  = 1h15

- projit testy ClientCanteenRelation, Client, ChainCanteen, zajistit jejich
  kompletnost
- otestovat StoredRelation, ktere nyni zavisi na current_client a vytvari
  relation pomoci safe_new, ktere testuje jeho existenci

=== Ruby ===
- when calling super, block provided to method is always passed on, to prevent
  this, call super(..., &nil)

module Mod
  def hello
    if defined?(super)
      super
    else
      "Good morning"
    end
  end
end
