=== Thu, 17.7. ===

- prace - fs
  - 11-11.30 - cistka kernelu
  - 4.30-7
    - zvazovani, zda by mel server-management kontrolovat validitu prichozich
      zprav, nebo by ji mel zajistovat message-server uz pri odesilani
      - mela by byt zodpovednost message-serveru, server-management by nemel
        resit
      - pokud by mel resit server-management, nema smysl definovat v
        message-serveru tridy pro Request a Response s danymi fieldy
      - staci resit pri odesilani, nebo nutne i pri prijeti? co kdyz napriklad
        odesle server se starou verzi message-serveru?

    - message-server:
      removed unused #to_hash from Message; fixed some comments

      Message.to_hash(object) now raises MissingField
      Producer now requires request to have response_queue
      fixed specs
      - adjusted message factories
      - adjusted fake handler in duplex communication to set required response fields

      Messages now validate data types and allowed values of their fields
      - added validate method to Message class and descendants
      - called by Encoding#encode

    - server-management:
      MessageHandler no longer needs to check message validity, since message-server
        does this upon message encoding
      - removed related specs
      added readers for logger, responder and config to MessageHandler

  - 12.30-2
    - server-management:
      extracted different aspects of message handling into listeners
      - introduced MessageHandlingListener as base, providing empty or abstract
        methods
      - introduced LoggingListener, RespondingListener and DebuggingListener as
        subclasses
      - initialized listeners in MessageHandler upon message receipt
      - moved handling to appropriate listeners
      - replaced by notifying them at each stage

  = 4h30
