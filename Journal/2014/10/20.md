=== Mon, 20.10. ===

- prace - fs - inventarizace
  - 2.15-5.30
    - hrani s vestal_versions a papertrail - zjisteni, co vse z vestal_versions
      volame
    - uprava zobrazeni historie u property na papertrail
    - zobrazeni historie pomoci js

    cleaned up property versioning views

    fixed Property#history displaying
    - added paper_trail gem
    - added migration to replace vestal_versions' versions table by PaperTrail's
    - added has_paper_trail definition to Property
    - added Property::Change structure
    - Property#history now returns array of these
    - added PropertiesHelper#render_change which selects appropriate partial for
      change type
    - updated views to accommodate these changes
    - added js and css to show/hide property history

    moved Property.changed_ids to summary
    fixed Property#history - inverted conditional

  - 6.15-8.15
    added rspec, guard and capybara

    regenerated some initializers

    regenerated other rails initial files
    - Rakefile, ApplicationController, config.ru, boot.rb
    - environment configs
    - error pages

    moved rspec-rails to development group
    initialized rspec
    removed generated application.js and .css

    updated spec helpers
    added spring-commands-rspec to run specs through spring
    generated Guardfile
    generated rspec binstub and added spring stuff

    added first feature spec
    excluded rails and capybara internals from backtraces
    added debugging gems
    fixed typo in ApplicationController

  - 10.30-11.15
    refactored create property spec
    - added 'users' shared context
    - used factories for account and users, added email to admin
    - added log_in_as helper
    - moved default_url_options to rails_helper
    - disabled loading of all support files
    + reseni problemu

  = 6h

== Versioning ==
- methods added to model by vestal_versions:
VestalVersions::Changes#methods: changes_between
VestalVersions::Reversion#methods: revert_to  revert_to!  reverted?  version
VestalVersions::Reset#methods: reset_to!
VestalVersions::Control#methods:
  _with_version_flag  append_version!  create_version?  merge_version!
  skip_version   update_version?
  append_version      append_version?  merge_version    merge_version?
skip_version!
VestalVersions::VersionTagging#methods: tag_version
VestalVersions::Reload#methods: reload
+ has_many :versions - association to versions table

- we use these:
versions, revert_to

property.versions => [version]
version.modifications => { attribute: [before, after] }
- before or after could be nil
- used in properties/version partial


== Ruby documentation ==
Generates all ri, rdoc and gems documentation for the current ruby:
    rvm docs generate

Generates only ri documentation for the current ruby:
    rvm docs generate-ri

Generates only rdoc documentation for the current ruby:
    rvm docs generate-rdoc

Generates only gems documentation for the current gemset:
    rvm docs generate-gems
