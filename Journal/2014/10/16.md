=== Thu, 16.10. ===

- prace - fs
  - 10-11.30 - servery
    - vycisteni starych kernelu na testingu, web1a, web1b, mysql1-master1,
      mysql1-slave1-1, mysql1-slave2-1, lb1, lb2, nix, akfrydlant
      - lb2 - problemy se zavislostmi - pro odinstalovani starych baliku je
        treba dokonfigurovat nove, na to ale neni dost mista - vyreseno na
        urovni dpkg

  - 11.45-1.15 - inventarizace
    upgraded rails to 4.1.6
    - updated gems, added spring
    - generated spring binstubs
    - set i18n.available_locales
    - moved secret_key_base from initializer to config/secrets.yml

    removed vestal_versions gem
    - versioning is disabled
    - removed initializer
    removed default_value_for gem
    - used after_initialize callbacks instead
    fixed Loan#only_one_active_loan_should_exist_on_one_property
    - ActiveRecord::Relation#compact was removed from API and was unneeded here
    cleaned up config/application

    updated prawn to 1.2.1, replaced prawnto by prawnto_2
    - export to pdf will need to be rewritten

  - 2.15-5.15 - inventarizace
    updated validations to rails 4 syntax
    used activevalidators gem instead of validatates_timeliness
    - replaced initializer
    - added quick hack to move date_validator above activevalidators in $LOAD_PATH
    - added localization
    moved date validation of Property#date_of_decommission to
      Decommission#date_of_assessment, which it belongs upon
    added date validations of Loan#lent_at, #returned_at and #expected_return_at
    added email validation of User#email
    Property#set_inventory_number is run only when account is set, otherwise causes
      error in before_validation callback
    added output of initializer load time in development mode
    updated tests

    updated timecop to 0.7.1

    posted bugreport for activevalidators (loading of DateValidator)
    - https://github.com/franckverrot/activevalidators/issues/71

    fixed moving of properties when deleting asset_types, collections and locations
    - @collection and @location wasn't set
    - used old .all(conditions: ...) syntax - changed to where
    - extracted finding from view to controller

  - 10-2 - rails template
    - testovani rails_apps_composeru
      - vygenerovani template
      - vygenerovani 2 projektu, hrani s nimi, customizace, testovani

  = 10h

== Rails templates - rails_app_composer ==
- devise, simple_form (initializer), cancancan, capistrano(rvm, bundler, rails),
  sass-rails, bootstrap, hb, wice_grid, kaminari, kaminari-boostrap, mysql2,
  therubyracer, jquery-rails, jquery-ui-rails
- development - annotate, better_errors, binding_of_caller, pry-rails, pry-doc,
  rakc-mini-profiler, letter_opener
- test - shoulda-matchers, factory_girl_rails, minitest/rspec, capybara

https://github.com/RailsApps/rails_apps_composer

- rails_apps_composer template ~/Desktop/template.rb -r git readme setup email
  gems tests frontend devise roles init locale deployment pages extras
- rails new -m
