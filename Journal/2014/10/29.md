=== Wed, 29.10. ===

- prace - fs
  - 2-3 - vosa - reseni joinovych scopes


SELECT bills.* FROM bills INNER JOIN
  (SELECT contract_id, max(created_at) FROM bills GROUP BY contract_id) AS sub
  ON bills.contract_id = sub.contract_id AND bills.created_at = sub.created_at
