=== Tue, 14.10. ===

- prace - fs - inventarizace
  - 3.30-6
    WIP: upgraded to rails 4.0.10
    - updated Gemfile
    - updated config files
    - added patch method to put routes
    - moved 3rd party assets to vendor/assets
    - removed scriptaculous javascripts since these are provided by prototype-rails
    - removed all.js

    disabled versioning, history and summary in Property
    - vestal_versions not working in rails 4
    - not setting @history in controller
    - not rendering history partial
    - disabled versioning tests in property_test
    - extracted summary tests from collection_test and disabled
    disabled validates_timeliness - doesn't support rails 4
    disabled ActiveRecord::Base#convert_number_column_value comma separator
      monkeypatch - method removed from ActiveRecord
    updated scopes to use lambdas instead of conditions hash
    removed include options from associations - deprecated
    fixed include options on finds
    removed ApplicationController#history and ApplicationHelper#history - not used

  - 6.30-8.30
    fixed ImportProperties test
    - added english localization for date import format
    - adjusted expected errors count

    added strong parameters to most controllers
    removed attr_accessible from models
    removed transitional gems - protected_attributes, caching
    in development unpermitted params now raise exception

    used before_action instead of before_filter in all controllers
    other minor changes, reformattings

    used authorize_resource instead of load_and_authorize_resource in all
      controllers, since resources are loaded manually

  = 4h30
