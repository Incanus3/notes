=== Tue, 7.10. ===

- prace - fs - inventarizace
  - 2.30-3.30
    replaced descend_by_id provided by searchlogic
    - using order(id: :desc)
    - reformatted properties/show view
    - extracted loans into partial (merged with loan partial)

    extracted history and links from properties/show into partials
    fixed html escaping of images

    replaced hideous property categories list by proper markup

    fixed properties/_form partial view
    - changed buttons to actions - new formtastic
    - added raw to hints, otherwise they get escaped
    - reformatted

    fixed collections/_form partial
    - replaced buttons by actions - new formtastic
    fixed flash rendering - ApplicationHelper#with_top_and_bottom_div

    fixed categories/index view
    - missing = for paginated_section
    - reformatted

  - 4.30-7
    disabled searching for categories/show
    fixed categories/form partial

    disabled searching for users#index
    fixed paginated section

    fixed users#edit
    - missing parentheses causing syntax error
    - replaced buttons by actions

    fixed showing of title
    - LyoutHelper#title set @content_for_title variable which was never used
    - used content_for(:title) instead

    disabled searching for locations#index and #show
    extracted partials links and properties from locations/show view
    fixed paginated section

    fixed PropertiesHelper#add_icons bv marking output as html safe

    fixed locations/form partial

    disabled searching for asset_types#index and show
    fixed asset_types/index view
    - paginated section, reformatted

    disabled searching in asset_types/show view
    extracted properties partial
    fixed paginated section
    reformatted

    fixed asset_types/form partial

    disabled searching in PropertiesController
    fixed properties/version partial view
    - used version[:modifications] instead of version[:changes] - column was rename d

    disabled searching for properties/archived

    fixed decommissions/form partial view
    simplified PropertiesController#archived

  - 10.15-10.45
    fixed problem with creating/editing users
    - problem was missing account_id in path - formerly resource_fu solved this by
      supplying @account_id
    - this means that account_id should be provided for every path we use, how do
      the other paths just work?

  = 4h


- problemy s predanim account_id (prvni cast url)
  - users - edit/update
  - properties - edit/update
  - decommissions - new/create
  - editace vypujcky
- nejspis zpusobeno vyhozenim resource_fu - ta zajistuje, ze chybejici segmenty
  cesty jsou nahrazeny hodnotami ze stejne pojmenovanych instvars, pokud
  existuji
- jak to, ze ale nektere formulare funguji v pohode? cesta ve zdrojaku stranky
  navic taky vypada v poradku
- rails v nekterych pripadech doplnuje parametry podle aktualniho requestu
- vyreseno definici default_url_options v ApplicationControlleru
