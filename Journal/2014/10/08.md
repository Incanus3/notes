=== Wed, 8.10. ===

- prace - fs - inventarizace
  - 1-2.30
    solved problem with missing account_id in paths globally
    - defined default_url_options in ApplicationController

    merged loan_table_row partial into properties/loans partial
    updated db/seeds

    fixed problem with cancel loan link not showing up
    - missing parentheses + && in erb template

    fixed loans/cancel_form and decommissions/result_form partials

  - 2.45-4
    fixed delete links by using prototype instead of jrails

    - hrani a problemy s archivaci - zatim nefunguje, zkusime po upgradu
    updated acts_as_archival
    deleted old gems and plugins in vendor

    added notes for @location.can_be_archived?
    added missing require to Location
    moved attr_reader :move_properties_to Nondeletable concern

  - 6.45-9.15
    added notes for deprecated AJAX calls
    - decommissions/{_quick_form,mass_index} views (reformatted)
    removed old rails2 backups
    - application_controller, preinitializer, session_store initializer, test_helper
    removed rails_upgrade plugin (the last one in vendor)

    removed old scripts
    removed public/index.html and rails.png used by it

    removed old <%=h escaping tags
    - rails 3 does this automatically

    recovered script/rails

    version bump

    upgraded to rails 3.1
    - updated gems - added assets group (sass, coffee, uglifier)
    - updated configs - added assets options
    - added initializer for parameter wrapping
    - request.requrest_uri is no longer available (lib/authentication)

    removed deprecated cache options from asset helpers
    - asset pipeline does this automatically

    upgraded to rails 3.2
    - updated gems, removed validation_reflection
    - added active_record options to config
    - required csv in import_properties

    upgraded to ruby 2.0.0

  - 11.15-11.30
    upgraded authlogic to 3.4.3
    updated db/seeds

    - upgrade CanCanu na 1.6.10, problem s load_and_authorize_resource nested:

  = 5h30

== Archivace sbírky ==
- with acts_as_archival 0.4.2 calling archive had no effect
- with 0.5.3 calling archive results in archived? => true, but this is reverted
  after reload
- 0.6.1 causes syntax error - probably needs newer ruby
