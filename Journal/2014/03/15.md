=== Sat, 15.3. ===

- prace - alto
  - 6.45-7.30
    - rebase test_meal_unordering on master, update rails
    - predelani single portion formulare na jednu routu - problem s routami
  = 1h15

- Hash#merge takes block |key,old_val,new_val| for keys that are in both hashes,
  its retval is the final value for the key
def deep_merge(h1, h2)
  h1.merge(h2) { |key, h1_elem, h2_elem| deep_merge(h1_elem, h2_elem) }
end
